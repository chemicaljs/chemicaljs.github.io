---
title: "Transport"
layout: "../../../layouts/Docs.astro"
---

# Transport

Switch the transport from the default set in [Setup](/docs/client/setup).

Use `libcurl` or `epoxy`.

```js
await chemical.setTransport("libcurl")
```